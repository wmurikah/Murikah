---
/** Container, centres content at the max page width with comfortable gutters. */
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<div class:list={['mx-auto w-full max-w-page px-5 sm:px-6 lg:px-8', className]}>
  <slot />
</div>
