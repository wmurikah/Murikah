/**
 * Placeholder for the full content seed.
 *
 * The real db/seed-content.ts is added by Build Prompt 4. It loads the page and
 * guide content and the first admin user. When implemented it MUST import the
 * shared loader (./_env) and use the shared client, exactly like db/seed.ts, so
 * it always runs the same way via `pnpm db:seed:content` and is never pasted
 * into a SQL console.
 *
 * Until then this is a no-op that does not touch the database or require any
 * credentials.
 */
console.log(
  'db:seed:content is a placeholder. The full content seed (pages, guides, first admin user) arrives with Build Prompt 4. Nothing was seeded.',
);
